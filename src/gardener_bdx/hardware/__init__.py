"""On-robot drivers. The :class:`~gardener_bdx.hardware.jetson_backend.JetsonBackend`
implements the same :class:`RobotIO` the simulators do, so the trained VLA +
locomotion + Guardian stack ports to the physical droid with no policy changes —
only these drivers get wired to real silicon."""
